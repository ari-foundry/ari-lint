// Main entry shell only. It returns success without reading process state,
// producing output, scanning source files, executing lint rules, calling
// tools/lint, or invoking ari --check.

fn run_main_entry_shell() -> i64 {
  return 0;
}

fn main() -> i64 {
  return run_main_entry_shell();
}
