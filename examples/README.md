# ari-lint Examples

Examples will be added after source extraction and standalone CLI wiring.

Ari code examples must be based on current `ari-foundry/ari` docs, examples,
and tests.

This directory intentionally does not include example commands yet because the
standalone `ari-lint` CLI is not wired in this repository. It also does not add
unverified Ari code examples in this skeleton step.
