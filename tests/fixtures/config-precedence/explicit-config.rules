# explicit --config fixture used by the initial strict config parity case.
# Broader precedence coverage should verify that explicit config disables discovery.
lint/trailing-whitespace = warning
lint/missing-final-newline = error
